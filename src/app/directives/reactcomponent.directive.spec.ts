import { ReactcomponentDirective } from './reactcomponent.directive';

describe('ReactcomponentDirective', () => {
  it('should create an instance', () => {
    const directive = new ReactcomponentDirective();
    expect(directive).toBeTruthy();
  });
});
